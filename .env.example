# !STARTERCONF Duplicate this to .env.local

# DEVELOPMENT TOOLS
# Ideally, don't add them to production deployment envs
# !STARTERCONF Change to true if you want to log data
NEXT_PUBLIC_SHOW_LOGGER="false"

# EMAIL CONFIGURATION FOR CONTACT FORM
# Email service configuration (using smtp.titan.email)
EMAIL_USER=your-email@yourdomain.com
EMAIL_PASS=your-email-password

# Optional: Frontend URL for any email links (if needed)
FRONTEND_URL=https://reboost.live

# Example configuration for smtp.titan.email:
# EMAIL_USER=info@reboost.live
# EMAIL_PASS=your_secure_password
# 
# Note: You'll need to set up an email account with a provider that supports SMTP
# such as smtp.titan.email, Gmail, or any other SMTP service
#
# For Gmail, you would use:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# 
# For smtp.titan.email:
# SMTP_HOST=smtp.titan.email
# SMTP_PORT=465